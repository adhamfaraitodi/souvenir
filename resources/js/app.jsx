import React from 'react';
import ReactDOM from 'react-dom/client';

const App = () => (
    <h1 className="text-4xl font-bold text-blue-500">Hello Laravel + React + Vite!</h1>
);

ReactDOM.createRoot(document.getElementById('app')).render(<App />);
